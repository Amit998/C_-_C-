
#include <iostream>


int main(){

    // int a=10,b=2;

    // std::cout<< a * b / 40 <<std::endl;

    // std::cout<< a % b  <<std::endl;

    // int a=20;

    // a +=20;
    // a *=20;

    // std::cout<< a<<std::endl;

    // int a=10;
    // a++;
    // ++a;
    // std::cout<< a<<std::endl;

    // int a=10,b=2;

    // std::cout<< (a < b)  <<std::endl;

    // int a=10,b=2;

    // std::cout<<(( a < b ) && ( a > 5)) <<std::endl;
    // std::cout<<(( a < b ) || ( a > 5)) <<std::endl;

    // std::cout<<((( a < b ) || ( a > 5)) && ( a > 5)) <<std::endl;

    int a=15;
    int b=11;
    


    



    return 0;
}